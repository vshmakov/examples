{{ include(
    'index/breadcrumbs/index.html.twig',
    {
        'breadcrumbs': [
        {'title': 'Мой профиль', 'url': path('account_index')},
        ]|merge(breadcrumbs ?? [])
    }
) }}