{{ include(
    'attempt/breadcrumbs/index.html.twig',
    {
        'breadcrumbs': [
            {'title': attemptResponse.title, 'url': path('attempt_show', {'id': attempt.id})}
        ]|merge(breadcrumbs ?? [])
    }
) }}