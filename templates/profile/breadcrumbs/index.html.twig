{{ include(
    'index/breadcrumbs/index.html.twig',
    {
        'breadcrumbs': [
        {'title': 'Профили настроек', 'url': path('profile_index')},
        ]|merge(breadcrumbs ?? [])
    }
) }}