{{ include(
    'profile/breadcrumbs/index.html.twig',
    {
        'breadcrumbs': [
        {'title': 'Создать профиль', 'url': path('profile_new')},
        ]|merge(breadcrumbs ?? [])
    }
) }}